"""Phase 5 CORE — deterministic substrate (CP5.1).

Implements the primitives frozen by ``docs/phase5-core-design-v6-4.md``:
canonical JSON (§3), digest/token grammar, the normative vocabulary (§4, §8, §9),
material projection (§14), the persistent state model and static validator (§11),
and virtual genesis (§12).

The frozen specification is the oracle. Nothing here reinterprets it.
"""
