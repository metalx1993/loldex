id: gtfobins/screen/file-write/sudo
type: binary
platform: linux
name: screen
phases:
- persistence
- privilege-escalation
capabilities:
- file-write
privilege_required: sudo
sources:
- project: GTFOBins
  upstream_url: https://gtfobins.github.io
  license: GPL-3.0
  upstream_version: acd5246
  last_synced: '2026-08-25'
commands:
- template: screen -L -Logfile /path/to/output-file echo DATA
  comment: Data is appended to the file and `\n` is converted to `\r\n`.
- template: screen -L /path/to/output-file echo DATA
  comment: Data is appended to the file and `\n` is converted to `\r\n`.
references:
- https://gtfobins.github.io/gtfobins/screen/
tags:
- linux
- unix
- gtfobins
