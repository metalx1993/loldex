id: gtfobins/find/file-read/unprivileged
type: binary
platform: linux
name: find
phases:
- collection
capabilities:
- file-read
privilege_required: user
sources:
- project: GTFOBins
  upstream_url: https://gtfobins.github.io
  license: GPL-3.0
  upstream_version: acd5246
  last_synced: '2026-08-25'
commands:
- template: find /path/to/input-file -exec cat {} \;
  comment: This uses `cat` to actually read the file, but since permissions are not dropped, it's executed
    with the same privileges as `find`.
references:
- https://gtfobins.github.io/gtfobins/find/
tags:
- linux
- unix
- gtfobins
