id: wadcoms/ldapsearch-creds
type: technique
platform: active-directory
name: LDAPSearch Creds
phases:
- discovery
capabilities:
- discovery
privilege_required: domain-user
sources:
- project: WADComs
  upstream_url: https://wadcoms.github.io
  license: GPL-3.0
  upstream_version: a864cd1
  last_synced: '2026-08-25'
preconditions:
- a valid username
- a valid password
commands:
- template: ldapsearch -h test.local -D 'ldap@test.local' -w password123 -b 'dc=test,dc=local'
  comment: 'ldapsearch is a Linux based tool that opens a connection to an LDAP server, binds, and performs
    a search using specified parameters. The following command will attempt to find sensitive information
    (such as leaked creds), by querying all LDAP objects, essentially dumping all the '
references:
- https://linux.die.net/man/1/ldapsearch
tags:
- active-directory
- wadcoms
- ldap
